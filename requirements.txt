# USHSS Portal — Python dependencies
# Based on app/req.txt, extended for FastAPI + JWT

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Templates & static files
jinja2>=3.1.0
python-multipart>=0.0.9

# Database (ORM)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# Auth
bcrypt>=4.1.0
python-jose[cryptography]>=3.3.0

# Validation (Pydantic v2 bundled with FastAPI)
pydantic[email]>=2.0.0

# Env config
python-dotenv>=1.0.0

# Production server
gunicorn>=21.0.0
